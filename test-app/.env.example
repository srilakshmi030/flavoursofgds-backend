# Base URL of the backend API. Baked in at build time by Vite.
# Local dev:  http://localhost:4000
# Sandbox:    https://<sandbox-api-host>
# Must be HTTPS for any Intune-delivered build.
VITE_API_BASE_URL=http://localhost:4000
